pragma solidity ^0.8.0;
library FractionalExponentsLibrary {
    // returns sorted token addresses, used to handle return values from pairs sorted in this order
    function power(uint256 _baseN, uint256 _baseD, uint256 _expN, uint256 _expD) internal pure returns (uint256, uint8) {
        // TODO
        return (1, 1);
    }
}
